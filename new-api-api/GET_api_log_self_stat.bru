meta {
  name: GET /api/log/self/stat
  type: http
  seq: 126
}

get {
  url: {{baseUrl}}/api/log/self/stat
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
