meta {
  name: DELETE /api/channel/disabled
  type: http
  seq: 96
}

DELETE {{baseUrl}}/api/channel/disabled

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
