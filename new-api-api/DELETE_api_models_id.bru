meta {
  name: DELETE /api/models/{id}
  type: http
  seq: 153
}

DELETE {{baseUrl}}/api/models/{id}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
