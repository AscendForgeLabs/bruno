meta {
  name: DELETE /api/channel/{id}
  type: http
  seq: 90
}

DELETE {{baseUrl}}/api/channel/{id}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
