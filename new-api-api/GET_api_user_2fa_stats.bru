meta {
  name: GET /api/user/2fa/stats
  type: http
  seq: 80
}

get {
  url: {{baseUrl}}/api/user/2fa/stats
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
