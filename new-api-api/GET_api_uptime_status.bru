meta {
  name: GET /api/uptime/status
  type: http
  seq: 5
}

GET {{baseUrl}}/api/uptime/status

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
