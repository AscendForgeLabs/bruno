meta {
  name: GET /api/channel/
  type: http
  seq: 83
}

GET {{baseUrl}}/api/channel/?p={{p}}&page_size={{page_size}}&id_sort={{id_sort}}&tag_mode={{tag_mode}}&status={{status}}&type={{type}}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
