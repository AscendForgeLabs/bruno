meta {
  name: GET /api/prefill_group/
  type: http
  seq: 134
}

get {
  url: {{baseUrl}}/api/prefill_group/
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
