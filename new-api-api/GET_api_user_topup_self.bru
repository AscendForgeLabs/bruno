meta {
  name: GET /api/user/topup/self
  type: http
  seq: 66
}

GET {{baseUrl}}/api/user/topup/self

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
