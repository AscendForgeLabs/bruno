meta {
  name: DELETE /api/redemption/{id}
  type: http
  seq: 121
}

delete {
  url: {{baseUrl}}/api/redemption/{id}
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
