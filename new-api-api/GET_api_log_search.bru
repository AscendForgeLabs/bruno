meta {
  name: GET /api/log/search
  type: http
  seq: 127
}

get {
  url: {{baseUrl}}/api/log/search?keyword={{keyword}}
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
