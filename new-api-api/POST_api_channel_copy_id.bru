meta {
  name: POST /api/channel/copy/{id}
  type: http
  seq: 106
}

post {
  url: {{baseUrl}}/api/channel/copy/{id}?suffix={{suffix}}&reset_balance={{reset_balance}}
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
