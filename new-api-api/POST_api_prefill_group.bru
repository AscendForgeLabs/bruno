meta {
  name: POST /api/prefill_group/
  type: http
  seq: 135
}

POST {{baseUrl}}/api/prefill_group/

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
