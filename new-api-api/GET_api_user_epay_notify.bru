meta {
  name: GET /api/user/epay/notify
  type: http
  seq: 72
}

get {
  url: {{baseUrl}}/api/user/epay/notify
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
