meta {
  name: GET /api/user/models
  type: http
  seq: 43
}

GET {{baseUrl}}/api/user/models

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
