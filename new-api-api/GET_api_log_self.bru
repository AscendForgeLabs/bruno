meta {
  name: GET /api/log/self
  type: http
  seq: 128
}

get {
  url: {{baseUrl}}/api/log/self
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
