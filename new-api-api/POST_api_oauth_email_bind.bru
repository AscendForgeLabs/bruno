meta {
  name: POST /api/oauth/email/bind
  type: http
  seq: 35
}

post {
  url: {{baseUrl}}/api/oauth/email/bind
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
