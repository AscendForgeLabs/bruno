meta {
  name: POST /api/user/aff_transfer
  type: http
  seq: 52
}

POST {{baseUrl}}/api/user/aff_transfer

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
