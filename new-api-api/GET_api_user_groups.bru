meta {
  name: GET /api/user/groups
  type: http
  seq: 25
}

GET {{baseUrl}}/api/user/groups

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
