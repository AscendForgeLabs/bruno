meta {
  name: POST /api/user/topup/complete
  type: http
  seq: 58
}

post {
  url: {{baseUrl}}/api/user/topup/complete
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
