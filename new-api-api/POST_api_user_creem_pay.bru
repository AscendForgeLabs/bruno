meta {
  name: POST /api/user/creem/pay
  type: http
  seq: 71
}

post {
  url: {{baseUrl}}/api/user/creem/pay
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
