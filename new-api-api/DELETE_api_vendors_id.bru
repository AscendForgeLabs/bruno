meta {
  name: DELETE /api/vendors/{id}
  type: http
  seq: 147
}

delete {
  url: {{baseUrl}}/api/vendors/{id}
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
