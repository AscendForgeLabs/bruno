meta {
  name: POST /api/user/manage
  type: http
  seq: 64
}

post {
  url: {{baseUrl}}/api/user/manage
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
