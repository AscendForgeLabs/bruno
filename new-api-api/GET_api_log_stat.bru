meta {
  name: GET /api/log/stat
  type: http
  seq: 125
}

GET {{baseUrl}}/api/log/stat

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
