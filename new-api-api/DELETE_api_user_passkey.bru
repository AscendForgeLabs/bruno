meta {
  name: DELETE /api/user/passkey
  type: http
  seq: 46
}

DELETE {{baseUrl}}/api/user/passkey

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
