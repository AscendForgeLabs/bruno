meta {
  name: GET /api/models/missing
  type: http
  seq: 156
}

get {
  url: {{baseUrl}}/api/models/missing
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
