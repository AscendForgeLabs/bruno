meta {
  name: GET /api/task/self
  type: http
  seq: 141
}

GET {{baseUrl}}/api/task/self

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
