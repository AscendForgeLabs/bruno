meta {
  name: GET /api/pricing
  type: http
  seq: 11
}

GET {{baseUrl}}/api/pricing

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
