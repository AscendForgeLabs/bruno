meta {
  name: GET /api/setup
  type: http
  seq: 1
}

GET {{baseUrl}}/api/setup

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
