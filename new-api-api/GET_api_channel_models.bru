meta {
  name: GET /api/channel/models
  type: http
  seq: 87
}

GET {{baseUrl}}/api/channel/models

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
