meta {
  name: GET /api/status/test
  type: http
  seq: 4
}

GET {{baseUrl}}/api/status/test

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
