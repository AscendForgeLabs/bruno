meta {
  name: POST /api/oauth/state
  type: http
  seq: 32
}

POST {{baseUrl}}/api/oauth/state

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
