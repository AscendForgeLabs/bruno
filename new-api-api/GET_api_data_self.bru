meta {
  name: GET /api/data/self
  type: http
  seq: 132
}

get {
  url: {{baseUrl}}/api/data/self
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
