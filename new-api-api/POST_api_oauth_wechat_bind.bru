meta {
  name: POST /api/oauth/wechat/bind
  type: http
  seq: 34
}

post {
  url: {{baseUrl}}/api/oauth/wechat/bind
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
