meta {
  name: GET /api/ratio_config
  type: http
  seq: 13
}

GET {{baseUrl}}/api/ratio_config

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
