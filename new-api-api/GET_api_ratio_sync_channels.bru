meta {
  name: GET /api/ratio_sync/channels
  type: http
  seq: 160
}

get {
  url: {{baseUrl}}/api/ratio_sync/channels
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
