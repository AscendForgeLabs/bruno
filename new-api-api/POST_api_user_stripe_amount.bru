meta {
  name: POST /api/user/stripe/amount
  type: http
  seq: 70
}

post {
  url: {{baseUrl}}/api/user/stripe/amount
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
