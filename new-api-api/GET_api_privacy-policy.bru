meta {
  name: GET /api/privacy-policy
  type: http
  seq: 8
}

GET {{baseUrl}}/api/privacy-policy

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
