meta {
  name: POST /api/user/login/2fa
  type: http
  seq: 19
}

post {
  url: {{baseUrl}}/api/user/login/2fa
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
