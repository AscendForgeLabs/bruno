meta {
  name: GET /api/oauth/github
  type: http
  seq: 28
}

GET {{baseUrl}}/api/oauth/github?code={{code}}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
