meta {
  name: POST /api/creem/webhook
  type: http
  seq: 74
}

post {
  url: {{baseUrl}}/api/creem/webhook
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
