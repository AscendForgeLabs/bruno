meta {
  name: GET /api/oauth/wechat
  type: http
  seq: 33
}

get {
  url: {{baseUrl}}/api/oauth/wechat
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
