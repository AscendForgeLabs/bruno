meta {
  name: POST /api/user/stripe/pay
  type: http
  seq: 69
}

POST {{baseUrl}}/api/user/stripe/pay

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
