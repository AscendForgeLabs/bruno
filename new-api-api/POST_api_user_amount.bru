meta {
  name: POST /api/user/amount
  type: http
  seq: 68
}

post {
  url: {{baseUrl}}/api/user/amount
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
