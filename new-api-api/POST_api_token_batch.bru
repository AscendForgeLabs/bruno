meta {
  name: POST /api/token/batch
  type: http
  seq: 114
}

post {
  url: {{baseUrl}}/api/token/batch
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
