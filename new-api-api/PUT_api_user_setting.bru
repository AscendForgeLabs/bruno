meta {
  name: PUT /api/user/setting
  type: http
  seq: 53
}

PUT {{baseUrl}}/api/user/setting

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
