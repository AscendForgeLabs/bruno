meta {
  name: GET /api/oauth/linuxdo
  type: http
  seq: 31
}

get {
  url: {{baseUrl}}/api/oauth/linuxdo
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
