meta {
  name: GET /api/user/topup/info
  type: http
  seq: 65
}

get {
  url: {{baseUrl}}/api/user/topup/info
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
