meta {
  name: GET /api/user/
  type: http
  seq: 55
}

GET {{baseUrl}}/api/user/?p={{p}}&page_size={{page_size}}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
