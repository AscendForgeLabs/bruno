meta {
  name: GET /api/verification
  type: http
  seq: 14
}

GET {{baseUrl}}/api/verification?email={{email}}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
