meta {
  name: POST /api/user/pay
  type: http
  seq: 67
}

post {
  url: {{baseUrl}}/api/user/pay
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
