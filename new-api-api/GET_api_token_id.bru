meta {
  name: GET /api/token/{id}
  type: http
  seq: 112
}

get {
  url: {{baseUrl}}/api/token/{id}
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
