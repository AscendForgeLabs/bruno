meta {
  name: POST /api/channel/batch
  type: http
  seq: 97
}

POST {{baseUrl}}/api/channel/batch

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json
{
  
}

tests {
  test("status is not server error", function() {
    expect(res.status).to.be.lessThan(500);
  });
}
